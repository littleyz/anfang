title: Security product information
description: security,surveillance,CCTV,IP camera and so on.

theme: minima
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
